name: music_streaming_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0" # Updated to support super-parameters

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  youtube_explode_dart: ^2.3.10
  just_audio: ^0.9.34
  just_audio_background: ^0.0.1-beta
  cloud_firestore: ^4.0.0
  firebase_core: ^2.0.0
  connectivity_plus: ^6.1.3
  shared_preferences: ^2.2.0
  rxdart: ^0.27.0
  audio_video_progress_bar: ^1.0.0  # For the progress bar widget
  cached_network_image: ^3.3.0
  dio: ^5.3.2
  path_provider: ^2.1.1
  permission_handler: ^11.4.0
  file_picker: ^10.0.0
  device_info_plus: ^9.1.2
  http: ^1.1.0
  flutter_web_auth_2: ^4.1.0
  url_launcher: ^6.1.14
  audio_session: ^0.1.16
  internet_connection_checker: ^1.0.0
  on_audio_query: ^2.9.0
  collection: ^1.17.0




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0 # Added for linting

flutter:
  uses-material-design: true